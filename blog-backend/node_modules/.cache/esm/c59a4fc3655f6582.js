let Post;_655‍.x([["write",()=>write],["list",()=>list],["read",()=>read],["remove",()=>remove],["update",()=>update]]);_655‍.w("../../models/post",[["default",["Post"],function(v){Post=v}]]);

// 블로그 포스트를 작성하는 API인 write 구현하기
       const write = async ctx => {
    const {title, body, tags}  = ctx.request.body;
    const post = new Post({
        title,
        body,
        tags
    });

    try {
        await post.save();
        ctx.body = post;
    } catch(e) {
        ctx.throw(500, e);
    }
};

       const list = ctx => {};

       const read = ctx => {};

       const remove = ctx => {};

       const update = ctx => {};
